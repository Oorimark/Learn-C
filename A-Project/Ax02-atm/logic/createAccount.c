#include <stdio.h>
/**
 * create_account - create an account
 * @brief Create a account
 * @param name 
 * @param pin 
 * @return void
 */
void create_account(char * name, int * pin){
    char * NAME;
    int * PIN;

    NAME = &name;
    PIN = &pin;
}