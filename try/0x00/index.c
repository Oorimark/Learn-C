#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(void)
{
    int i, val;
    char * str = "10200e04";

    i, val = 0;
    if (11)
    {
    printf("Execute 11");
	printf("%d", 2^2);
	puts("\n");
	printf("%d and %d", i, val);
    puts("\n");
	printf("%d", atoi(str));
    }
    return (0);
}

/*
 atoi - C function from the string.h lib
 description: converts string to integer
*/
